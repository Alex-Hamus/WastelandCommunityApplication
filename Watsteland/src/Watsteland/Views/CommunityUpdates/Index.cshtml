@model IEnumerable<Watsteland.Models.CommunityUpdates>

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.UdateInformation)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.UserId)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.UserName)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.UdateInformation)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.UserId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.UserName)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.CommunityUpdateId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.CommunityUpdateId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.CommunityUpdateId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
